# Accepted-risk waivers for the Trivy CI gate (apps/*/Dockerfile image scans).
#
# Rules for this file:
#   - Every entry MUST have a reason AND a revisit condition.
#   - Only waive what is genuinely not fixable today. Prefer fixing.
#   - Keep it short. Re-check on each dependency-update sweep.

# CVE-2026-42504 — Go stdlib MIME-header DoS, compiled into the Caddy binary
# that ships in caddy:2-alpine (built with Go 1.26.3; the fix is Go 1.26.4).
#
# Why waived: we do not compile Caddy ourselves, and as of 2026-06-15 the
# newest published image (caddy:2.11.4 / the floating 2-alpine tag) is still
# built on Go 1.26.3 — there is no patched Caddy image to upgrade to. The
# finding is a denial-of-service (not RCE), and the edge sits behind Cloudflare.
#
# Revisit: the Dockerfile uses the floating `caddy:2-alpine` tag, so CI will
# pull the rebuilt image automatically once Caddy republishes on Go 1.26.4+.
# REMOVE this line then (the CVE will simply stop appearing).
CVE-2026-42504

# CVE-2026-27145 — Go stdlib crypto/x509 VerifyHostname quadratic-time DoS, in
# the same caddy:2-alpine Caddy binary (Go 1.26.3; fix is Go 1.26.4 / 1.25.11) as
# CVE-2026-42504 above.
#
# Why waived: same root cause and same blocker — no Caddy image built on Go
# 1.26.4+ is published yet (CI's fresh pull of the floating 2-alpine tag still
# resolves to Go 1.26.3 as of 2026-06-23). DoS only (NVD CVSS 6.5, Integrity:None);
# it bites only a TLS *client* that connects to an attacker-controlled server
# presenting a cert with a huge DNS SAN list. Caddy here only serves static files
# + reverse-proxies our own API behind Cloudflare, so it never makes a TLS-client
# call to an attacker-controlled host — no reachable path.
#
# Revisit: clears together with CVE-2026-42504 once Caddy republishes the floating
# 2-alpine tag on Go 1.26.4+. REMOVE both lines then.
CVE-2026-27145

# CVE-2026-12151 — undici DoS (unbounded memory) in undici 6.24.1, pulled
# transitively by discord.js (@discordjs/rest) for the Nuggie bot. Hoists to the
# root node_modules, so it appears in all three images' scans.
#
# Why waived: discord.js pins undici 6.x and there is no non-breaking root bump —
# an npm `overrides` to 6.27.0 does not reach the workspace lockfile that `npm ci`
# installs from, and force-regenerating the lock risks broad version drift right
# before deploy. The finding is a denial-of-service (not RCE), and the bot's
# undici only makes outbound calls to Discord's own API — no attacker-controlled
# host is reachable through it.
#
# Revisit: remove once discord.js ships a release depending on undici >= 6.27.0
# (then `npm update discord.js` and this line stops matching).
CVE-2026-12151
